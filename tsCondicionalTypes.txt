//conditional types | operador ternario

// type Condicional<T> = T extends string ? "É uma string" : "Não é uma string"

// function conditional<T>(value: T): Condicional<T>{
//     const text = typeof value === "string"
//     ? "É uma string"
//     : "Não é uma string"
//     return text as Condicional<T>;
// }


// const result = conditional("Joka")

// class SelectMenu {};
// class Checkbox {};
// class Input {};
// class Button {};

// type ElementType = "selectmenu" | "checkbox" | "input" | "button"

// type GetElement<T> = 
//     T extends "selectmenu" ? SelectMenu :
//     T extends "checkbox" ? Checkbox :
//     T extends "input" ? Input :
//     T extends "button" ? Button :
//     never;


// interface Data<T extends ElementType> {
//     type: T
//     execute(element: GetElement<T>): void
// }

// /**const data: Data<"selectmenu"> = {
//     execute(element) {
//         element;
//     },
// } */

// function CreateData<T extends ElementType>(data: Data<T>){
   
// }

// CreateData({
//     type: "checkbox",
//     execute(element) {
        
//     },
// })


type IsString<T> = T extends string ? true : false
type IsNumber<T> = T extends number ? true : false

interface StringOptions {
    upper(): void;
    lower(): void;
}

interface NumberOptions {
    pow(exp: number): number;
    sub(b: number): number;
    sum(b: number): number;
}


type ManipuleOptions<T> = 
    IsString<T> extends true ? StringOptions :
    IsNumber<T> extends true ? NumberOptions :
    never;

    function manipule<T>(value: T): ManipuleOptions<T>{
        return Object.create({})
    }


    manipule("Joka")

